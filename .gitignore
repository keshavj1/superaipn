# dependencies
node_modules/

# build output
dist/
dist-ssr/
*.local

# secrets — never commit
.env
.env.*
!.env.example
*.env

# large media (host on a CDN, not in the repo)
# Videos ship with the site: Vercel builds from this repo, so ignoring them
# deployed a site with every <video> empty. All are individually <100MB.

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
logs/
*.log

# editor / OS
.vscode/*
!.vscode/extensions.json
.idea/
.DS_Store
Thumbs.db
